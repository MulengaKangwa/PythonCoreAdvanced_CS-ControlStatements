# Command lines are used to find file locations, databases and Remote IPs.

import sys # This imports the sys module.
lst= sys.argv #

for i in lst: print(i) #  The print is displayed on same line because it one line.

print(len(lst))

print(lst[0])
